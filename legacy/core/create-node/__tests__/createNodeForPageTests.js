test('createNodeForPage creates node of type Page', () => {});

test("createNodeForPage creates node whose content is the document's slug", () => {});

test("createNodeForPage creates node whose content digest is the digest of the document's slug", () => {});

test('createNodeForPage creates node with a parent-child relationship built with the given parent node.', () => {});

test('createNodeForPage creates node with node-id of pattern: page-${document.slug}', () => {});
