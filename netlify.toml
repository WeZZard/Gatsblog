## Netlify configuration for blog (wezzard.com)

[build]
  publish = "public"

[[edge_functions]]
  function = "flex_redirect"
  path = "/*"

## Short-link logic is handled by Edge Function `flex_redirect` for both
## root and nested forms: /l/:code and */l/:code
